<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sohailhaider.omlate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ChatRoom" />

        <service android:name=".SignalRService" />

        <activity android:name=".MainMenuActivity" />
        <activity android:name=".AllCoursesActivity" />
        <activity android:name=".NewCourseDetails" />
        <activity android:name=".EnrolledCourses" />
        <activity android:name=".AllAssessments" />
        <activity android:name=".AssessmentDetails" />
        <activity android:name=".Quiz"></activity>
    </application>

</manifest>